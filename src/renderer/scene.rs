// use super::node::Node;

// pub struct Scene {
//     root: Node,
// }

// impl Scene {
//     pub fn run(&self) {
//         // execute every node iteratively
//         // how do we ensure there's no cycles?
//         self.root.attachment.execute();

//         for child in self.root.children() {}
//     }
// }
